# P7 compatibility corpus

These thirty small, authored documents exercise common public-input shapes. They are intentionally
licensed with the repository and are compiled as a batch in `packages/compiler/tests/compatibility.test.ts`.
The corpus is not a claim that every possible Markdown dialect is supported; it is a reproducible
regression baseline for the documented format.
